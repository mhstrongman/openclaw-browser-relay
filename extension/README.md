# Extension

This repo does not include the extension source.

Install the official OpenClaw Browser Relay extension via:

```bash
openclaw browser extension install
openclaw browser extension path
```

Then load unpacked from the printed path in Chrome (`chrome://extensions`).
